# 🏗️ Construction Management System

A modular web app built with Flask for managing construction projects, staff, vehicles, materials, finance, and more.

## 🚀 Setup

1. Clone/extract this project
2. Run `run.bat` (Windows) or:

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py
